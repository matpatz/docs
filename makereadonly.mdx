---
title: "makereadonly"
---

```luau
makereadonly(t: table): ()
```

Acts as a shortcut for `setreadonly(t, true){:luau}`. Once locked, `rawset{:luau}` and direct key assignment both fail.

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `t` | `table` | The table to lock. |

## Returns

<code title="the function does not return a value.">void</code>

## Example
```

Locks a table so its contents can't be changed.

luau
local cfg = {speed = 16}

makereadonly(cfg)
cfg.speed = 100 --> errors: attempt to modify a readonly table
```

## Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `t` | `table` | The table to lock. |

## Returns

<code title="the function does not return a value.">void</code>

## Example

<CodeGroup>

```luau Example 1
makereadonly(t: table): ()
```

```luau Example 2
local cfg = {speed = 16}

makereadonly(cfg)
cfg.speed = 100 --> errors: attempt to modify a readonly table
```

</CodeGroup>

<Tip>
  Always lock metatables back to read-only after editing them to avoid detection.
</Tip>
