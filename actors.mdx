---
title: "create_comm_channel"
---

```luau
function create_comm_channel(): number, BindableEvent
```

**Example:**

<CodeGroup>

```luau Example 1 highlight={1}
local id, channel = create_comm_channel()
channel.Event:Connect(function(data)
    print(data) -- Hello, World!
end)

run_on_actor(getactors()[1], [[
    local id = ...
    local channel = get_comm_channel(id)
    channel:Fire("Hello, World!")
]], id)
```

```text Example 2
```

</CodeGroup>

The Bindable Event created will not be parented inside game, and cannot be destroyed. The ID will be equal to the amount of communication channels.

<Check>
  This function can be implemented in Lua.
</Check>

```luau
local channels = {}

local function create_comm_channel(): (number, BindableEvent)
	local Id = #channels + 1
	local Event = Instance.new("BindableEvent", gethui())

	channels[Id] = {
		Id = Id,
		Event = Event
	}

	return Id, Event
end

local function get_comm_channel(Id: number): BindableEvent?
	local Channel = channels[Id]
	return Channel and Channel.Event
end

local function get_comm_channels()
	return channels
end
```